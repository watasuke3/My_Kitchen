# CLAUDE.md

## プロジェクト概要

**My Kitchen** は、ユーザーの身体情報（性別・身長・体重・年齢・活動レベル）に基づいて最適な1週間の献立を作成・管理する献立管理Webアプリ。Scala学習を兼ねた個人開発プロジェクト。

### 技術スタック

- **バックエンド**: Scala 2.13 + Play Framework 3.0, Slick (DB), Flyway (マイグレーション), sbt
- **フロントエンド**: TypeScript + React 19 + Vite 8
- **DB**: PostgreSQL 17
- **開発環境**: WSL2 (Ubuntu 24.04)

## 開発方針

- 学習目的のプロジェクトのため、各フェーズで実行したコマンドや実装の理由を `学習の軌跡/` 配下に学習ログとして記録する
- 実装中に出てきた新しい構文・イディオム（Scalaの構文、Play/Slickの書き方、Reactのフックなど）は、その都度対応する学習ログファイルに解説を書き込む
- ユーザーとのコミュニケーションは日本語で行う
- コミット・pushは勝手に行わない。実行はユーザーの承認を得てから行う
- 大きな変更に着手する前に、対応するPhaseの学習ログに方針をメモしてから実装する
- Scala側は例外を握りつぶさず、エラーは意味のある型（`Either` / `Option` / カスタムエラー）で表現する
- パスワードやAPIキーなど機密情報は `.env` に置き、コミット対象に含めない
- 未使用のimport・変数は残さない
- 実装した理由や比較検討した選択肢（例: JWT vs Cookieセッション）を学習ログに残す
- 新しいライブラリ・概念を使うときは一言解説を学習ログに書く
- 不明点・設計判断が必要な箇所は実装前にユーザーに確認する
- セキュリティ要件（docs/SPEC.md より、Phase 4以降で継続的に意識すること）:
  - パスワードは bcrypt または argon2 でハッシュ化（平文保存禁止）
  - 全通信の HTTPS 化
  - セッション管理は HttpOnly + Secure Cookie、または JWT（短命アクセストークン + リフレッシュトークン）
  - XSS / CSRF 対策（フレームワーク標準機構の活用、入力のエスケープ・サニタイズ）
  - ログインAPIへのレート制限（ブルートフォース対策）
  - パスワードポリシー: 最低8文字、英数字を含む

## 実装Todoリスト

### Phase 0: 環境構築 ✅ 完了

- [x] WSL2 (Ubuntu 24.04) 上に Scala / Java / Docker 環境を構築

### Phase 1: バックエンド Hello World ✅ 完了

- [x] Play Framework のひな形作成
- [x] sbt run で起動確認
- [x] Scala 2.13 へ変更

### Phase 2: フロントエンド Hello World ✅ 完了

- [x] Vite + React のひな形作成
- [x] npm run dev で起動確認

### Phase 3: フロントとバックを HTTP で繋ぐ ✅ 完了

- [x] /api/health エンドポイント作成
- [x] CORS 設定
- [x] フロントエンドから fetch で連携確認

### Phase 4: ユーザー登録・ログイン 🔶 実装済み・動作確認とコミットが未完了

- [x] バックエンドAPI実装（`AuthController.scala`, `User.scala`, `UserRepository.scala`, `SessionRepository.scala`, `AuthService.scala`）
- [x] DBマイグレーション作成（`backend/conf/evolutions/`）
- [x] フロントエンド実装（`AuthContext.tsx`, `ProtectedRoute.tsx`, `Login.tsx`, `Register.tsx`）
- [x] 学習ログ作成（`学習の軌跡/Phase4_認証実装.md`）
- [ ] 動作確認・テスト（登録 → ログイン → 保護ページアクセスの一連の流れ）
- [ ] セキュリティ要件の充足確認（パスワードハッシュ化、セッション管理、パスワードポリシー等）
- [ ] git コミット

### Phase 5: レシピCRUD 🔶 実装済み・動作確認とコミットが未完了

- [x] バックエンドAPI実装（`RecipeController.scala`, `Recipe.scala`, `RecipeRepository.scala`, `RecipeService.scala`）
- [x] フロントエンド実装（`recipes.ts`, `RecipeList.tsx`, `RecipeForm.tsx`, `RecipeDetail.tsx`）
- [x] 学習ログ作成（`学習の軌跡/Phase5_レシピCRUD.md`）
- [ ] 動作確認・テスト（レシピの作成・一覧・詳細・更新・削除）
- [ ] git コミット

### Phase 6: カレンダー・献立管理 / 買い物リスト ⬜ 未着手

- [ ] `meal_plans`, `shopping_lists` テーブル設計・マイグレーション
- [ ] ウィークリープラン（朝・昼・晩）の献立割り当てカレンダーUI
- [ ] 重複回避（直近1〜2週間に登場した献立を自動提案から除外、設定可能）
- [ ] 食事実績記録（「食べた」ボタン）
- [ ] スキップ管理（「食べなかった」フラグ、栄養集計から除外）
- [ ] 過去の食事実績の履歴参照
- [ ] 買い物リスト: 基本食材のプリセット
- [ ] 買い物リスト: ユーザー追加アイテム
- [ ] 買い物リスト: チェック管理（購入済み一括クリア）
- [ ] 買い物リスト: 複数端末同期
  - 注: レシピはURL登録のため、献立からの食材自動生成機能はなし（手動追加運用）

### Phase 7: 栄養計算・フィードバック ⬜ 未着手

- [ ] Ganpule の式による BMR（基礎代謝）計算
- [ ] PAL（活動レベル係数）を掛けた必要カロリー計算
- [ ] 目標設定（ダイエット / 維持 / 筋力アップ）に応じた PFC バランス目標値算出
- [ ] レシピ内の数値化された具材・調味料を基にした栄養価のリアルタイム算出
- [ ] 必須栄養素充足率の提示
- [ ] ユーザー指定タイミング（食後 / 1日の終わり / 週末等）でのフィードバック表示（「食後」は「食べた」ボタン押下がトリガー）

### Phase 8: アレルギー除外・人数スケーリング・重複回避の強化 ⬜ 未着手

- [ ] アレルギー・嫌いな食材登録機能、献立提案・自動生成からの除外
- [ ] レシピの人数（分量）スケーリング（基準人数から任意人数への換算）
- [ ] 献立自動生成時の重複回避ロジックの本実装

### Phase 9（将来）: 本番デプロイ ⬜ 未着手

- [ ] HTTPS 化（Nginx / CDN）
- [ ] デプロイ先選定（Render / Fly.io / VPS 等）
- [ ] 環境変数の本番用管理
